# Инструкция по деплою на GitHub Pages

## ✅ Что было сделано

### 1. Конфигурация Next.js для статического экспорта
- ✅ Добавлен `output: 'export'` в `next.config.mjs`
- ✅ Настроен `basePath` и `assetPrefix` для проектных страниц GitHub Pages
- ✅ Добавлены домены Shopify в `remotePatterns` для изображений
- ✅ Изображения настроены как `unoptimized: true` для статического экспорта

### 2. GitHub Actions Workflow
- ✅ Создан `.github/workflows/deploy.yml` для автоматического деплоя
- ✅ Настроена автоматическая сборка при push в `main`/`master`
- ✅ Настроен деплой на GitHub Pages

### 3. Обновления кода
- ✅ Закомментированы `revalidate` директивы (не поддерживаются в статическом экспорте)
- ✅ Исправлена ошибка с параметрами `limit`/`first` в `getProducts`
- ✅ Обновлен `.gitignore` для исключения ненужных файлов

### 4. Документация
- ✅ Обновлен `README.md` с инструкциями по деплою
- ✅ Добавлены разделы по troubleshooting

## 📋 Что нужно сделать перед деплоем

### Шаг 1: Настройка GitHub Pages

1. Перейдите в настройки репозитория: `Settings` → `Pages`
2. В разделе `Source` выберите **"GitHub Actions"**
3. Сохраните изменения

### Шаг 2: Настройка секретов (опционально)

Если у вас есть свой Shopify Store Domain:

1. Перейдите в `Settings` → `Secrets and variables` → `Actions`
2. Нажмите `New repository secret`
3. Имя: `NEXT_PUBLIC_SHOPIFY_STORE_DOMAIN`
4. Значение: ваш домен (например: `your-store.myshopify.com`)
5. Нажмите `Add secret`

**Примечание:** Если секрет не указан, будет использован домен по умолчанию `v0-template.myshopify.com`

### Шаг 3: Запуск деплоя

#### Автоматический деплой:
- Просто сделайте `git push` в ветку `main` или `master`
- Workflow автоматически запустится и задеплоит сайт

#### Ручной запуск:
1. Перейдите в `Actions` в вашем репозитории
2. Выберите workflow `Deploy to GitHub Pages`
3. Нажмите `Run workflow`
4. Выберите ветку и нажмите `Run workflow`

### Шаг 4: Проверка деплоя

1. После завершения workflow, перейдите в `Settings` → `Pages`
2. Там будет указан URL вашего сайта
3. Обычно это: `https://username.github.io/repository-name` (для проектных страниц)
   или `https://username.github.io` (для пользовательских страниц)

## ⚠️ Важные замечания

### Ограничения статического экспорта:

1. **ISR не поддерживается** - все страницы генерируются на этапе сборки
2. **Server Actions не работают** - только статические страницы
3. **API Routes не работают** - используйте внешние API
4. **Динамические маршруты** - должны быть предварительно сгенерированы через `generateStaticParams`

### Текущая конфигурация:

- ✅ Все продукты и коллекции генерируются статически через `generateStaticParams`
- ✅ Используется Shopify Storefront API (без токенов, публичный доступ)
- ✅ Корзина работает через клиентские компоненты и cookies

## 🔧 Локальная разработка

```bash
# Установка зависимостей
npm install

# Запуск dev сервера
npm run dev

# Тестовая сборка для проверки статического экспорта
npm run build

# Просмотр production сборки локально
npx serve out
```

## 🐛 Troubleshooting

### Проблема: Workflow не запускается
- Проверьте, что файл `.github/workflows/deploy.yml` существует
- Убедитесь, что вы пушите в ветку `main` или `master`
- Проверьте права доступа в `Settings` → `Actions` → `General`

### Проблема: Ошибка сборки
- Проверьте логи в `Actions` → выберите workflow → `build` job
- Убедитесь, что все зависимости указаны в `package.json`
- Проверьте, что Node.js версия совместима (используется версия 20)

### Проблема: Сайт не обновляется
- GitHub Pages может кэшировать старую версию (подождите 1-2 минуты)
- Очистите кэш браузера (Ctrl+Shift+R)
- Проверьте, что workflow успешно завершился

### Проблема: Изображения не загружаются
- Проверьте, что домены Shopify добавлены в `remotePatterns` в `next.config.mjs`
- Убедитесь, что CORS настроен правильно на стороне Shopify
- Проверьте консоль браузера на наличие ошибок

## 📝 Дополнительная информация

- **Base Path:** Автоматически определяется на основе имени репозитория
- **Trailing Slash:** Добавлены для совместимости с GitHub Pages
- **Environment Variables:** Используются только `NEXT_PUBLIC_*` переменные (доступны в браузере)

## 🎉 Готово!

После выполнения всех шагов, ваш сайт будет автоматически деплоиться при каждом push в основную ветку.

